sudo: required

language: sh

addons:
  apt:
    sources:
      - ppa:jonathonf/vim
    packages:
      - vim

script:
  - ./setup.sh
  - source ~/.bashrc
  - vim +'PlugInstall --sync' +qall

after_script:
  - vim --version

after_failure:
  - id
  - pwd
  - df -h

notifications:
  email:
    on_success: never
